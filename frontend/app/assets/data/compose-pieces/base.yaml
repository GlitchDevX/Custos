services:
  custos:
    image: glitchdevx/custos:latest
    restart: always
    ports:
      - "127.0.0.1:<<BACKEND_PORT>>:3060"
    volumes:
      - type: bind
        source: ./custos-config
        target: /config
<<UI_PART>>volumes:
  custos: